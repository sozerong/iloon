# Airflow 이미지 전용 의존성.
#
# requirements.txt 를 그대로 설치하면 안 된다 — 거기 있는 sqlalchemy>=2.0 이
# Airflow 2.9.3 이 고정해 둔 SQLAlchemy 1.4 를 덮어써서 `airflow db migrate` 가
# MappedAnnotationError 로 죽는다 (CLI의 users 서브커맨드도 사라진다).
#
# DAG 태스크가 실제로 쓰는 것만 남긴다:
#   pyspark        analyze_*.py
#   psycopg2-binary analyze_* 의 PostgreSQL 적재
#   anthropic      generator_*.py
# fastapi/uvicorn/sqlalchemy/asyncpg/httpx/ollama 는 ai-server 전용이라 뺐다
# (ai_server/Dockerfile + ai_server_requirements.txt 가 따로 있다).
pyspark==3.3.4
psycopg2-binary>=2.9.0
anthropic>=0.49.0
